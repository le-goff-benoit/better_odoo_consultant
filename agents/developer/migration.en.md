### Migration specifics (Developer mode)
- List of **ORM/XML breaking changes** between source and target versions.
- Renamed / removed methods or fields, deprecated decorators.
- Migration scripts (pre/post) and non-regression tests to plan.
- Suggested format: `Element | vSource | vTarget | Migration action`.
